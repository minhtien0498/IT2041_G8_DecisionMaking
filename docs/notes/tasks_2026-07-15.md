# Task tồn đọng sau khi Solution 2 chạy trên bộ chung

Cập nhật: `2026-07-15` · Nguồn: Quang (Solution 2) · Hạn chốt: `24/07`

## Trạng thái

Solution 2 đã chạy trên dataset 100 căn + 10 case `V1_*` của Ấn, 10/10 case `ok`,
output đúng contract tại `outputs/solution2_results.json`. case_id đã trùng Solution 1
nên ghép bảng compare được.

Đối chiếu sơ bộ: 5/10 case hai solution ra Top 5 giống hệt (đều là case free-text
không thêm tiêu chí đo được). Chỉ lệch ở `V1_006`, `V1_007`, `V1_008`, `V1_010`.

---

## T1 · Chạy lại enrichment 10 căn thiếu
**Owner:** Tiến · **Ưu tiên:** Cao · **Chặn:** kết quả compare cuối

- File `go_vap_tan_binh_100_enriched_overpass_api.json` chỉ có 90/100 căn.
- Thiếu đúng khối liên tục `TB_041`–`TB_050`; `overpass_errors.json` rỗng `[]`.
- Nghi job enrich đứt giữa chừng, không phải API lỗi từng căn.

**Cần làm**
- [ ] Chạy lại enrich cho `TB_041`–`TB_050`
- [ ] Sửa nhánh ghi `errors.json` để job đứt thì có log, tránh thiếu dữ liệu âm thầm

---

## T2 · Chốt một nguồn dữ liệu tiện ích duy nhất
**Owner:** Tiến · **Ưu tiên:** Cao · **Chặn:** `base_score` của cả 2 solution

- `go_vap_tan_binh_100_enriched.json` (đang dùng chung) và bản Overpass lệch nhau lớn:
  `distance_to_nearest_school_m` chênh trung vị `664m`, max `3153m`.
- Ví dụ `GV_001`: file chung `1601m` tới trường gần nhất, Overpass `219m`.
- Nghi file chung tính từ POI nhập tay nên khoảng cách xa hơn thực tế.
- Check chéo: query Overpass của Solution 2 cho `TB_001` ra `market 259m`, khớp đúng
  số trong file Overpass của Tiến.

**Cần làm**
- [ ] Chốt nguồn chuẩn cho dataset chung (đề xuất: bản Overpass)
- [ ] Cập nhật lại file dùng chung theo nguồn đã chốt

---

## T3 · Chốt chấm điểm trên X + Y
**Owner:** Ấn · **Ưu tiên:** Cao · **Liên quan:** đề xuất của Phú

- Solution 2 chấm `final = 0.7·X + 0.3·Y` với X = tiện ích có sẵn trong dataset,
  Y = tiện ích suy từ free-text rồi enrich thêm.
- Nếu ground truth chỉ xếp hạng theo X, cả 2 solution đều bị trừ điểm vì đã làm
  đúng phần Y.

**Cần làm**
- [ ] Chốt ground truth xếp hạng theo `X + Y`
- [ ] Ghi rõ cách diễn dịch Y từ free-text vào rubric

---

## T4 · Đổi `user_need_text` sang tiếng Việt có dấu
**Owner:** Ấn · **Ưu tiên:** Trung bình · **Liên quan:** yêu cầu của Phú

- Bỏ dấu làm `"chợ"` → `"cho"`, trùng giới từ `"cho"` và danh từ `"chỗ"`.
- Bộ V1 dính bẫy này ở 3 chỗ: `"de cho thue lai"` (V1_003),
  `"du cho 2 vo chong"` (V1_005), `"mam non cho con"` (V1_006).
- Solution 2 đã xử lý được, nhưng input có dấu bỏ được nhập nhằng ngay từ gốc,
  tốt cho cả 2 solution.

**Cần làm**
- [ ] Viết lại `user_need_text` của 10 case thành tiếng Việt có dấu

---

## T5 · Bổ sung case kiểm tra được phần free-text
**Owner:** Ấn · **Ưu tiên:** Trung bình

- Bộ V1 hiện chỉ có 4/10 case có nhu cầu thêm đo được (`V1_006`, `007`, `008`, `010`).
- 4 case free-text chỉ nhắc lại tiêu chí đã có trong form, 2 case chủ quan.
- Đây là hành vi đúng thiết kế, nhưng làm compare ít thông tin.

**Cần làm**
- [ ] Thêm 2-3 case có nhu cầu đo được nhưng chưa có trong form
      (chợ, cafe, nhà thuốc, gym, mầm non)

---

## T6 · Dọn naming Solution 1 còn sót
**Owner:** Phú · **Ưu tiên:** Thấp

- `docs/Solution-1-Detail.md` đã cập nhật đúng (2-LLM pipeline).
- Còn 3 file vẫn ghi Solution 1 = MCDA/TOPSIS: `docs/README.md`,
  `docs/solution_evaluation.md`, `docs/notes/solution_scope_change_2026-07-11.md`.
- Slide/report bám các file này sẽ mô tả sai việc của Phú.

**Cần làm**
- [ ] Cập nhật 3 file trên cho khớp source thực tế

---

## T7 · Review explanation của Solution 1
**Owner:** Quang · **Ưu tiên:** Trung bình · **Yêu cầu từ:** Phú

- Phú đã push `outputs/explanations/` (10 file `V1_*.md`), nhờ đọc và so với Solution 2.

**Cần làm**
- [ ] Đọc 10 file explanation, phản hồi chất lượng so với Solution 2
